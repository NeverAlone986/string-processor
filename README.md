# String Processor

## Описание
Программа реализует:
- Генерацию случайных строк.
- Фильтрацию строк, содержащих символ "a", с помощью goroutine и каналов.

## Установка и запуск
```sh
git clone <repo_url>
cd string-processor
make all
